platform: linux
image_resource:
  type: docker-image
  source:
    repository: lachlanevenson/k8s-kubectl
    tag: "latest"
run:
  path: sh
  args:
  - -exc
  - |
    kubectl config set-cluster local-cluster --server=http://ae04df29.ngrok.io
    kubectl config set-context local-context --cluster=local-cluster
    kubectl config use-context local-context
    kubectl --namespace=k8s apply -f _manifests/gateway-deploy.yaml
